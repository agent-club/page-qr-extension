# 1.0.1 上架准备验证

日期：2026-09-07。

- `npm run check`：通过，插件运行脚本语法检查无误。
- `npm test`：9 项核心测试通过，覆盖二维码解码与输入边界。
- `npm run test:browser`：在 Chrome for Testing 147.0.7727.15 中 11 项检查通过，使用独立临时配置；涵盖实际工具栏动作、离线生成、PNG／SVG 下载回读、外观存储和失败场景。
- `npm run package:store`：通过。安装包为 `page-qr-extension-1.0.1.zip`，根目录直接包含 Manifest V3 清单，11 个运行文件，22,310 字节。
- ZIP SHA-256：`c5b9070b5aa5b0bbf443d954829b83a241e78c4bfc31a64a58913335a2130f2a`。
- 官网 `npm run build`、`npx tsc --noEmit`：通过；本地首页、隐私政策和安装包下载请求返回 HTTP 200。
- 官网已成功部署第 1 版，访问范围暂为仅维护者可见；公开访问必须在提审前完成并检查。
- 商店截图为 1280 × 800，小型宣传图为 440 × 280，图标为 128 × 128；尺寸已读取验证，截图基于实际插件界面生成。

Chrome 102 为清单声明的最低版本，此次没有在该旧版本实测。官网未进行浏览器视觉验收。尚未创建商店条目、提交审核或取得审核结果。此文件记录工程验证，不代表 Chrome 网上应用店审核通过。
